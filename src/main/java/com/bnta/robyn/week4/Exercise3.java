package com.bnta.robyn.week4;

public class Exercise3 {
    //split input
    // create a stack<String>
    //loop
    // if is "( or [ or {" stack.push(bracket)
    // if closing
    // check if stack is empty if is return false if not
    // check if bracket matches top of stack then pop (remove)
    // else closing bracket does not match keep in stack return false to boolean
    // return stack.isEmpty()

}
